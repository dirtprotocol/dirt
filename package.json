{
  "name": "smart",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "rebuild": "docker-compose down && docker-compose up --build contracts",
    "deploy": "docker-compose run contracts npm run migrate",
    "unpause": "docker-compose unpause ganache",
    "pause": "docker-compose pause ganache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
